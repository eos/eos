# Results of the Analysis "Inference Example Notebook"

## Posteriors

The analysis defines {{ analysis_file.posteriors | length }} posterior{{ 's' if analysis_file.posteriors | length > 1 }}:

{% for name in analysis_file.posteriors %}
- `{{ name }}`
{% endfor %}

{% for name in analysis_file.posteriors %}
### Posterior `{{ name }}`

This posterior involves {{ analyses[name].varied_parameters | length }} varied parameter{{ 's' if analyses[name].varied_parameters | length > 1 }}:

{% for parameter in analyses[name].varied_parameters %}
- `{{ parameter.name() }}`
{% endfor %}

{% endfor %}

## Results

The 2D marginal posterior density for the parameter $|V_{cb}|$ (`CKM::abs(V_cb)`) and $f_+(0)$ (`B->D::alpha^f+_0@BSZ2015`) is shown in the following figure:

![corner_figure](inference-data/fig-Vcb-v-fp0.pdf)
